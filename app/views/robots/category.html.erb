
<%= render "shared/navbar" %>

<h1 class="text-center mt-5"><%= @category.capitalize %> Robots</h1>

<div class="d-flex justify-content-between">
  <div class="row m-5" style="width: 66.66%;">
    <% if @robots.present? %>
      <% @robots.each do |robot| %>
        <div class="col-md-6 mb-4">
          <div class="card">
           <%= image_tag(robot.robot_pictures, style: "width:400px; height:400px")%>
            <div class="card-body w-20 ">
              <h5 class="card-title"><%= link_to robot.robot_name, robot_path(robot), class: "text-decoration-none" %></h5>

            </div>
          </div>
        </div>
      <% end %>
    <% end %>
  </div>
  <div class="card" style="width: 33.33%;">

  </div>
</div>
